-- Delete all entries from the users table
DELETE FROM users;

-- Delete all entries from the portfolios table
DELETE FROM portfolios;

-- Delete all entries from the groups table
DELETE FROM groups;

-- Delete all entries from the users_to_groups table
DELETE FROM users_to_groups;

-- Delete all entries from the groups_to_portfolios table
DELETE FROM groups_to_portfolios;

-- Delete all entries from the portfolios_to_stocks table
DELETE FROM portfolios_to_stocks;
